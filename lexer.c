#include "lexer.h"

//returns amount of tokens in string
int tokens_counter(char* src)
{
    int i = 0; 
    int token_amount = 0;
    
    while (src[i] != '\0')
    {

        i += 1;
    }

    return 
}